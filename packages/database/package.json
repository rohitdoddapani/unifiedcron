{
  "name": "@unifiedcron/database",
  "version": "1.0.0",
  "description": "Database schema and migrations for UnifiedCron",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "migrate": "node dist/migrate.js",
    "migrate:dev": "ts-node src/migrate.ts"
  },
  "dependencies": {
    "@unifiedcron/shared": "workspace:*",
    "dotenv": "^16.6.1",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/pg": "^8.10.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
