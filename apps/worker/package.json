{
  "name": "@unifiedcron/worker",
  "version": "1.0.0",
  "description": "UnifiedCron background worker for monitoring and alerting",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "test": "jest"
  },
  "dependencies": {
    "node-cron": "^3.0.3",
    "dotenv": "^16.3.1",
    "@unifiedcron/shared": "workspace:*",
    "@unifiedcron/database": "workspace:*",
    "@unifiedcron/integrations": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "@types/node": "^20.10.5",
    "@types/node-cron": "^3.0.11",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.8"
  }
}
