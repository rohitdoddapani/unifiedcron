# Documentation Notes

The canonical docs are published at https://docs.unifiedcron.com.

This `docs/` folder contains the source markdown that is mirrored into the Nextra site in `docs-site/pages/`. If you update a guide here, update the corresponding page in `docs-site` as well.
